srun:
    parameters:
        ntasks: -n
        nodes: -N
        reservation: --reservation
        cores per task: -c

mpirun:
    parameters:
        ntasks: -n
        nodes: -N
        cores per task: -c
